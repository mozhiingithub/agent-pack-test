# 执行计划模板

> 复制本模板到 `active/<分支名>.md`。合入 main 后归档到 `completed/`。

## 来源需求

（指向 `docs/product-specs/` 中的文件；纯修复类写"无"）

## 目标

（这条分支要交付什么，一句话）

## 触及的既有模块

（声明会改动/依赖哪些既有文件或模块；新功能尽量新增文件）

## 部署影响

（新增/修改的环境变量、端口、依赖、构建参数；无则写"无"。export 脚本硬性检查本节必须存在）

## 验收标准

- [ ] 本地检查（`bash tools/check.sh`）全绿
- [ ] （如涉及对外行为/接口）已同步更新 `docs/design-docs/`
- [ ] 内网 latest 实测通过

## 任务拆分（checkbox，执行时逐项勾选）

> 每个任务须可独立测试交付，粒度以"一次专注完成"为宜；禁止占位符（TBD/"适当处理"）。

- [ ] 任务 1：（做什么、动哪些文件、怎么验证）
- [ ] 任务 2：…
